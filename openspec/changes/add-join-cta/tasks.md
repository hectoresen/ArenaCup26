# Tasks — add-join-cta

- [ ] 1. Componente `GoogleSignInButton` en `src/components/auth/google-sign-in-button.tsx` con el logo "G" SVG inline (4 colores oficiales) y stub `onClick`.
- [ ] 2. Componente `JoinModal` en `src/components/auth/join-modal.tsx`: backdrop blureado, card `max-w-[400px]`, título h2 con id, subtítulo p con id, `GoogleSignInButton`, footer privacidad, X close, manejo de Escape, click fuera, body scroll lock, focus inicial en X.
- [ ] 3. Componente `JoinCta` en `src/components/auth/join-cta.tsx`: botón gold "Predecir ahora" y estado open/close del modal.
- [ ] 4. Integrar `<JoinCta />` en `LeaderboardView` como bloque propio entre el `<header>` y el podio.
- [ ] 5. Promover `specs/auth-entry/spec.md` a `openspec/specs/auth-entry/spec.md`.
- [ ] 6. Mover propuesta a `archive/`.
- [ ] (deferred) Tests con React Testing Library cuando se añada al stack en una propuesta de tooling.
