# Tasks — add-account-picker-prompt

- [ ] 1. En `src/lib/auth.ts`, añadir `authorization.params.prompt = "select_account"` al provider Google.
- [ ] 2. Verificar manualmente: login → logout → click "Continuar con Google" → debe mostrar selector de cuentas (no auto-login).
- [ ] 3. Update `docs/decisions.md` (próxima sección §16) con la decisión y trade-off.
